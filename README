This is the git repository of autoreport plugin for ducati.

do a [ git log ] to see the logs of all the commits
do a [ git pull origin master ] to update/merge your local working directory with the remote repository
do a [ git status ] to see which files has been modified in your local working directory
do a [ git add filename ] for each file you want to be commited
do a [ git rm filename ] to remove a file
do a [ git mv file1 file2 ] to move a file
do a [ git commit -m "message that will be associated with your commit" ] to commit your changes (on your local dir)
do a [ git push origin master ] to upload your commit on the  remote repository

you can practice on the TEST file

-- Muri
